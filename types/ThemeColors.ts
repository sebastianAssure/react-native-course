export interface ThemeColors {
  background: string;
  text: string;
  buttonText: string;
  primary: string;
  secondary: string;
  accent: string;
  icon: string;
  border: string;
  statusBar: 'light-content' | 'dark-content' | 'default';
}
